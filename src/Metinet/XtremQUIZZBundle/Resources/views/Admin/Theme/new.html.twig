{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Nouveau theme</h1>

    <form action="{{ path('admin_theme_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Créer</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_theme') }}">Retour</a>
    </li>
</ul>
{% endblock %}
